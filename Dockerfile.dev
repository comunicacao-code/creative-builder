# Dockerfile para desenvolvimento com hot-reload
FROM node:20-alpine

WORKDIR /app

# Copiar apenas os arquivos de dependências primeiro (cache layer)
COPY package.json package-lock.json* ./

# Instalar dependências
RUN npm ci

# Copiar o resto do código
COPY . .

# Expor a porta do Vite
EXPOSE 5173

# Comando padrão: iniciar o servidor de desenvolvimento
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
